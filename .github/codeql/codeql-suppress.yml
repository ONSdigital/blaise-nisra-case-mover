# .github/codeql/codeql-suppress.yml
name: "Custom CodeQL config"
disable-default-queries: false

# leave the default queries untouched
queries: []

# suppress rules
query-filters:
  - exclude:
    id: py/paramiko-missing-host-key-validation
    reason: Using AutoAddPolicy intentionally in prod/dev; required by current setup.
